const config = {
    database: {
        host: '127.0.0.1',
        port: '27017',
        db: 'zemoso',
        user: encodeURIComponent("root"),
        pwd: encodeURIComponent("dungeon@[2015H]")
    }

};

export default config;
